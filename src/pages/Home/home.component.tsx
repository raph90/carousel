import React, { FC } from "react";
import Carousel from "./relatedComponents/Carousel/carousel.component";

const Home: FC = () => {
  return (
    <div className="home">
      <Carousel />
    </div>
  );
};

export default Home;
